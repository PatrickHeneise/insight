require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe BulletActivitiesController do

  #Delete this example and add some real ones
  it "should use BulletActivitiesController" do
    controller.should be_an_instance_of(BulletActivitiesController)
  end

end

<h1>Listing enrollments</h1>

<table>
  <tr>
  </tr>

<% @enrollments.each do |enrollments| %>
  <tr>
    <td><%= link_to 'Show', enrollments %></td>
    <td><%= link_to 'Edit', edit_enrollments_path(enrollments) %></td>
    <td><%= link_to 'Destroy', enrollments, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New enrollments', new_enrollments_path %>
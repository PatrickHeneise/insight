<%= error_messages_for :folder %>
<% form_for([:admin,@folder]) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
	<%= f.hidden_field :parent_id %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>